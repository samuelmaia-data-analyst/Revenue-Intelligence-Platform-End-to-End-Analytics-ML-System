# Changelog

## 0.1.0 - 2026-03-11

- packaged the project with `pyproject.toml`
- added the `ridp` CLI and `ridp-dashboard` entry points
- introduced schema validation and clearer pipeline failure modes
- expanded regression coverage for contracts and invalid inputs
- added CI, `pre-commit`, `Makefile`, `.env.example`, and repository templates
